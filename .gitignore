# compiled output
/node_modules

# Use yarn as the single package manager; keep yarn.lock committed for
# reproducible builds (Docker/Coolify), and ignore the npm lockfile.
package-lock.json

# Production
/build

# Generated files
.docusaurus
.cache-loader

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
