name: notion-mcp-server-sse
version: 0.0.1
schema: v1
mcpServers:
  - name: notion-mcp-server
    type: http
    url: https://mcp.notion.com/sse
    auth:
      type: bearer
      token: "${NOTION_API_KEY}"
      # As above, rely on NOTION_API_KEY from environment; rotate if previously exposed.
